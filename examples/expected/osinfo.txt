bare prefix:
  os.PLATFORM = linux
  os.ARCH     = amd64
  os.DIRSEP   = /
  os.PATHSEP  = :
aliased prefix (`use os as o;`):
  o.PLATFORM  = linux
  o.ARCH      = amd64
aliased prefix (`use os as convert;`):
  convert.PLATFORM = linux
  convert.ARCH     = amd64
