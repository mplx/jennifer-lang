# Jennifer modules

Jennifer-coded library modules (`.j` files) live here. Unlike the Go
system libraries (`internal/lib/*`, enabled with `use NAME;`), a module is
distributable Jennifer source, brought in with `import "NAME.j" as NAME;`.

Distribution packages install these to the system module directory
(`/usr/share/jennifer/modules/` by default; see `jennifer version -v`),
so `import "NAME.j";` resolves without a path. Local modules resolve with
`import "./NAME.j";`, and extra search directories are added with
`jennifer run -I DIR ...`.

## Available modules

The **TinyGo** column says whether a module runs on the constrained
`jennifer-tiny` binary. A `No` module needs the default `jennifer` binary: it
uses `net`, and the stock `jennifer-tiny` ships no network stack. A `Partial`
module runs on both binaries except for its network-backed calls, which need the
default binary. Every module
name links to its reference doc under [`docs/modules/`](../docs/modules/index.md);
a runnable demo for each lives in [`examples/modules/`](../examples/modules/).

| Module | TinyGo | Description |
| ------ | ------ | ----------- |
| [`ansi.j`](../docs/modules/ansi.md) | Yes | Terminal styling as explicit string wrappers: `color` / `bgColor` / `style` / `rgb` / `strip` plus per-colour and per-style shortcuts (`ansi.red`, `ansi.bold`, ...). TTY-aware: suppresses itself off a terminal or under `NO_COLOR`, forced on by `FORCE_COLOR`. |
| [`bucket.j`](../docs/modules/bucket.md) | No | S3-compatible object storage (AWS S3 / MinIO / Cloudflare R2 / Backblaze B2), every request AWS Signature V4-signed: `connect(endpoint, region, key, secret)` -> `Client`, then `get` / `put` / `delete` / `listObjects` (+ `objectKeys` to parse the listing XML). Path-style; one module, configurable endpoint. Over `hash.hmac` + `hash.compute` + `encoding` + `http` + `time`. |
| [`cron.j`](../docs/modules/cron.md) | Yes | Cron schedules: `parse(expr)` -> `Schedule`, `matches(schedule, t)` -> bool, and `next(schedule, after)` -> `time.Time` (next fire at or after a time). Five standard fields with `*` / `,` / `-` / `/n`, day-of-week `0-7`, and the dom-OR-dow rule. A pure calculator over `time`; a scheduler loop is the caller's. |
| [`csv.j`](../docs/modules/csv.md) | Yes | RFC 4180 comma-separated values: `parse` / `format` (`parseWith` / `formatWith` for any delimiter, TSV too), and `toRecords` / `fromRecords` for header-keyed `map of string to string`. Quoting-aware. |
| [`docblock.j`](../docs/modules/docblock.md) | Yes | The Jennifer doc-comment format and its parser. `docblock.parse(source)` returns a typed `FileDoc` tree (module preamble, per-construct docs); reports doc drift (a `@param` / `@field` naming nothing real, a parameter with no `@param`) and orphaned comments as `Diagnostic`s. Over `regex` / `strings`. |
| [`dotenv.j`](../docs/modules/dotenv.md) | Yes | Read `.env` config files: `parse(text)` / `read(path)` -> `map of string to string`, and `load(path)` (parse + set each var via `os.setEnv`). Handles `#` comments (whole-line + inline), blank lines, a leading `export`, and single- / double-quoted values (double quotes expand `\n` / `\t`). Over `fs` + `strings` + `os`. |
| [`flatdb.j`](../docs/modules/flatdb.md) | Yes | A file-backed JSON document store over `json` + `fs`: `open` a file into a value-semantic `DB`, query / edit by JSON Pointer (`get` / `has` / `keys` / `length`; fresh-`DB` `set` / `append` / `remove`), `save` with a crash-atomic temp + `rename`. Not a database engine - snapshotting of small data. |
| [`htmlwriter.j`](../docs/modules/htmlwriter.md) | Yes | Build an HTML element tree and render escaped HTML5: `element` / `text` / `raw` / `attr` constructors, `render` / `renderAll`, `escape`. Void-element aware. A writer, not a parser. |
| [`http.j`](../docs/modules/http.md) | No | An HTTP/1.1 client over `net` (`https://` via TLS): `request` plus `get` / `post` / `put` / `patch` / `delete` / `head` / `options` -> `Response` (`status`, `headers`, `body`), with `header` case-insensitive read. Content-Length + chunked; redirects returned, not followed. |
| [`gotify.j`](../docs/modules/gotify.md) | No | Push a notification to a [Gotify](https://gotify.net) server, on top of `http`: `push(cfg, title, message, priority)` POSTs the message form with the `X-Gotify-Key` header. Value-semantic `Config` (url + token), caller-supplied. |
| [`gpio.j`](../docs/modules/gpio.md) | Yes | Raspberry-Pi (and any Linux SBC) GPIO over sysfs, with `fs` as the backend. Stateless, pin-keyed: `setup(pin, "in"/"out")`, `write(pin, 0/1)`, `read(pin)`, `release(pin)`. Root `/sys/class/gpio`, overridable via `JENNIFER_GPIO_BASE`. Off a GPIO host, calls throw `Error{kind: "gpio"}` cleanly. |
| [`rest.j`](../docs/modules/rest.md) | No | An ergonomic REST layer over `http` + `json`: a value-semantic `Client` (base URL + headers) and `get` / `post` / `put` / `patch` / `delete` -> `Response`, plus `getJson` / `postJson` / `putJson` / `patchJson`. Base-URL joining, query strings, `bearer` / `basic` / `withHeader` auth. |
| [`oauth.j`](../docs/modules/oauth.md) | No | A generic OAuth2 client (the get-a-token half) over `http` + `json`: client-credentials, refresh-token, and device-authorization grants; `google` / `microsoft` presets; `isExpired` + on-disk token store. Tokens feed `sasl` XOAUTH2. |
| [`web.j`](../docs/modules/web.md) | No | A small HTTP framework over the `httpd` engine: register routes against handler methods by name (`get` / `post` / ...), `:param` capture, `before` middleware, `notFound`; a handler takes a `web.Context` (request accessors + response helpers), dispatched via `meta.callMain`. `web.run($app, addr)`; run with `jennifer serve app.j [--watch]`. |
| [`markdown.j`](../docs/modules/markdown.md) | Yes | Render a CommonMark subset to HTML (through `htmlwriter`) and to styled terminal text (through `ansi`) with `toHtml` / `toAnsi`; plus Markdown authoring helpers (`header` / `style` / `link` / `bullets` / `numbered` / `codeBlock` / `table`) and `tablePretty` to align table source. |
| [`memcache.j`](../docs/modules/memcache.md) | No | A memcached client (classic text protocol) over `net`: `set` / `add` / `get` / `delete` / `incr` / `decr` / `touch`, every store with a TTL. A volatile cache (for caches, sessions, counters, locks), not a system of record. |
| [`session.j`](../docs/modules/session.md) | No | Server-side sessions on `memcache`: a `map of string to string` under `sess:ID` with a sliding TTL. `create` / `load` / `save` / `touch` / `destroy`; UUID v4 ids, base64-wrapped JSON values so any UTF-8 round-trips. Volatile. |
| [`ratelimit.j`](../docs/modules/ratelimit.md) | No | A fixed-window rate limiter on `memcache` (atomic `incr` + per-key TTL): `allow(mc, key, limit, window)` -> bool, `remaining(mc, key, limit)`. The window resets on its own when it expires. |
| [`mime.j`](../docs/modules/mime.md) | Yes | Build and parse MIME messages (RFC 5322 headers, multipart, quoted-printable / base64, RFC 2047 encoded-words for non-ASCII headers): `text` / `attachment` / `multipart` / `encode` / `parse` (+ `headerValue` / `body` / `parts` / `contentType`). No networking; the foundation the mail clients build on. |
| [`sasl.j`](../docs/modules/sasl.md) | Yes | Crypto-free SASL auth mechanisms as base64 encoders, shared by the mail clients: `plain` / `loginUser` / `loginPass` / `bearer` (SASL XOAUTH2 - the use-a-token half of OAuth2). No networking, no crypto. |
| [`semver.j`](../docs/modules/semver.md) | Yes | Strict Semantic Versioning 2.0.0, package-registry-grade: `parse` / `isValid` / `toString`, `compare` / `lt` / `lte` / `eq` / `neq` / `gt` / `gte` / `diff`, `isStable` / `isPrerelease`, `incMajor` / `incMinor` / `incPatch`, `sort` / `rsort`; `coerce` / `clean` for loose tags; plus npm/Composer range matching - `satisfies` (caret / tilde / comparators / `\|\|` OR / hyphen / x-ranges, with npm prerelease rules), `maxSatisfying` / `minSatisfying` / `minVersion` / `validRange`, and solver algebra `intersects` / `subset` / `gtr` / `ltr` / `outside` / `simplifyRange` (prerelease-precise). Exported `Version` struct. |
| [`smtp.j`](../docs/modules/smtp.md) | No | Send mail (SMTP client) over `net`: `send(opts, from, recipients, message)` runs the RFC 5321 dialogue (EHLO, optional STARTTLS / implicit TLS, `AUTH PLAIN`, `MAIL FROM` / `RCPT TO` / `DATA`); message built by `mime`. Throws `Error{kind: "smtp"}` on rejection. |
| [`tengine.j`](../docs/modules/tengine.md) | Yes | A lightweight-CMS text template engine (a subset of Go `text/template`, the Go / Hugo style) rendered over a `json.Value` data tree: `newSet` / `add` / `render`. `.path` / `$` root / `$var` addressing; `if` / `else if` / `else` with `eq` / `ne` / `lt` / `gt` / `and` / `or` / `not` (parenthesised nesting); `range` (with `$i, $e :=`) / `with` / `block`; `{{ $x := PIPE }}` variables; `define` / `template` layout inheritance; `{{/* comments */}}`; `{{- -}}` whitespace-trim markers; and output pipes `upper` / `lower` / `title` / `trim` / `html` / `urlize` / `default` / `truncate` / `join` / `len` / `printf`. No compile step. Over `json` / `strings` / `lists` / `maps` / `convert`. |
| [`totp.j`](../docs/modules/totp.md) | Yes | Time-based one-time passwords (RFC 6238 over RFC 4226 HOTP): `generate` / `verify` (current time, +/-1-step skew) and deterministic `generateAt` / `verifyAt`, plus `uri` for the `otpauth://` provisioning string a QR code encodes. base32 secrets; SHA-1 default, SHA-256 / SHA-512 via `Options`. Over `hash.hmac` + `encoding` + `time`. |
| [`webhook.j`](../docs/modules/webhook.md) | Partial | HMAC-signed webhooks (GitHub `X-Hub-Signature-256`): `sign(payload, secret)` -> `sha256=<hex>` and constant-time `verify(payload, signature, secret)` are pure (both binaries); `send(url, payload, secret)` POSTs the signed body over `http` (default binary). Over `hash.hmac` + `encoding` (hex). |
| [`imap.j`](../docs/modules/imap.md) | No | Receive mail (IMAP4rev1, RFC 3501) over `net`, a reading subset: `connect` -> `Session`, then `selectMailbox` / `search` / `fetch` / `logout`, plus `fetchAll`. Handles tagged responses and `{N}` literals; messages are strings for `mime.parse`. |
| [`idna.j`](../docs/modules/idna.md) | Yes | Internationalized domain names: `toAscii` / `toUnicode` over a Punycode (RFC 3492) core (`münchen.de` <-> `xn--mnchen-3ya.de`), plus `isAscii`. No networking; the mail clients IDNA-encode hosts and envelope domains through it. |
| [`pop.j`](../docs/modules/pop.md) | No | Receive mail (POP3, RFC 1939) over `net`: `connect` opens a session, then `stat` / `count` / `sizes` / `retrieve` / `deleteMessage` / `quit`, plus `fetchAll`. Messages are strings for `mime.parse`; throws `Error{kind: "pop3"}` on `-ERR`. |
| [`redis.j`](../docs/modules/redis.md) | No | A Redis client speaking RESP2 over `net`: typed helpers `get` / `set` / `del` / `exists` / `incr` / `decr` / `keys` / `ping`, plus a generic `command(session, args)` -> `Reply` for anything else. `connect` does optional `AUTH` / `SELECT`; a `-ERR` throws `Error{kind: "redis"}`. |
| [`resque.j`](../docs/modules/resque.md) | No | Background jobs on Redis, wire-compatible with Resque: `enqueue` onto named queues, `reserve` the next `Job` in priority order, plus `queueLength` / `queues` / `size` / `fail`. Interops with Ruby-resque / php-resque workers. Built on `redis` + `json`. |
| [`mqtt.j`](../docs/modules/mqtt.md) | No | An MQTT 3.1.1 pub/sub client over `net` (`mqtts` via TLS): `connect` -> `Client`, then `subscribe` / `publish` / `publishBytes` (QoS 0), blocking `receive` and `poll(client, timeoutMs)` (single-threaded, via `net.setDeadline`), `ping`, `disconnect`. Binary packet framing built with bitwise ops + `bytes`. |
| [`label.j`](../docs/modules/label.md) | Partial | Industrial label printing in a build / render / emit pipeline. Build a device-independent `Label` in millimetres (`new` / `text` / `barcode` / `box` / `image` / `quantity`; text rotation / point sizes / bold; barcodes code128 / ean13 / ean8 / itf / code39 / gs1-128 / datamatrix / qr), `render(label, device)` to a selectable dialect built with `zpl(dpi)` / `cab()` / `cabWith(setup)` (`"zpl"` Zebra, `"cab"` cab JScript with a cab-only `CabSetup` for the J/H/O/S print-setup), then `send(host, port, rendered)` to a printer's raw `:9100` port. Build / render run on both binaries; `send` needs the default binary. |
| [`prometheus.j`](../docs/modules/prometheus.md) | Partial | Prometheus metrics in two halves. **Exposition** (`counter` / `gauge` / `observe` / `render`) builds a metric set and renders the text format - pure text, both binaries. **Retrieval** (`query` / `queryRange` -> `Result`) is a read client for the HTTP query API over `http` + `json`, so it needs the default binary. Strict name / label validation and escaping. |
| [`log.j`](../docs/modules/log.md) | Partial | Leveled, structured logging: a `Logger` carries a minimum level (`debug` < `info` < `warn` < `error`), a format (`text` / `logfmt` / `json`), and a sink; `debug` / `info` / `warn` / `error` (and `at` for a runtime level) render one record - timestamp, level, message, `map of string to string` fields - and write it, dropping records below the level. Sinks: `new` (stdout) / `toStderr` / `toFile` / `toSyslog` (RFC 5424 over UDP). Console + file work on both binaries; the syslog sink needs the default binary. |
| [`ical.j`](../docs/modules/ical.md) | Yes | iCalendar (RFC 5545) build and parse: a `Calendar` of `Event`s encoded to a `VCALENDAR` of `VEVENT`s (`calendar` / `event` / `describe` / `locate` / `add` value-semantic builders, `encode` / `parse`). `DTSTAMP` / `DTSTART` / `DTEND` go through `time` (UTC `DATE-TIME`); text values RFC 5545-escaped, long lines folded, so `parse(encode(cal))` round-trips. `VEVENT`-only (no `RRULE` / `VALARM` / `TZID`). Pure text over `strings` / `lists` + `time`; both binaries. Shares the content-line codec (`ical_vcard_shared.j`) with `vcard`. |
| [`vcard.j`](../docs/modules/vcard.md) | Yes | vCard (RFC 6350, vCard 4.0) contacts build and parse: a `Card` of contact fields encoded to a `VCARD` and parsed back (`card` / `withName` / `withOrg` / `addEmail` / `addPhone` / `address` / `addAddress` / `withUrl` / `withNote` value-semantic builders, `encode` / `encodeAll` / `parse`). Structured `N` / `ADR` / `ORG`, RFC 6350 escaping and 75-char folding - shares the content-line codec (`ical_vcard_shared.j`) with `ical`. A contact subset (no `BDAY` / `PHOTO` / parameter round-trip). Pure text over `strings` / `lists`; both binaries. |
| [`jsonl.j`](../docs/modules/jsonl.md) | Yes | JSON Lines (JSONL / NDJSON): newline-delimited JSON, one `json.Value` per line. `encode` / `decode` (compact JSON split / joined on `\n`, blank lines skipped, so `decode(encode(rows))` round-trips), whole-file `readFile` / `writeFile` / `appendFile`, and a streaming `Reader` (`openReader` / `hasMore` / `readRecord` / `closeReader`) for files too large to hold in memory. A thin framing layer over `json` + `fs`; both binaries. |
| [`ipnet.j`](../docs/modules/ipnet.md) | Yes | IP addresses and CIDR networks, IPv4 and IPv6. `parseAddress` / `toString` (canonical, RFC 5952 for IPv6) / `version` / `equal`; `parse(cidr) -> Network` (host bits zeroed), `contains(net, addr)`, `netmask` / `broadcast` / `networkString`. Addresses held as raw `bytes` (4 / 16); bitwise subnet math for allow-lists and membership. Pure `.j` over `strings` + `convert`; both binaries. |
| [`ntp.j`](../docs/modules/ntp.md) | No | A simple SNTP network-time client (RFC 4330 / 5905) over UDP: `query(host)` / `queryWith(address, timeoutMs)` -> `Result` (`serverTime` + clock `offset` + round-trip `delay`). Packs / unpacks the 48-byte NTP packet with `bytes` + bitwise ops, converts the NTP epoch through `time`, and bounds the receive with a UDP deadline so a lost reply times out rather than hangs. Query-only (no clock discipline / daemon). Needs the default binary (net). |
| [`pdfwriter.j`](../docs/modules/pdfwriter.md) | Yes | Generate simple PDF documents (text / lines / rectangles): `document` / `page` / `text` / `line` / `rect` / `color` / `addPage` value-semantic builders, `info` document metadata (+ `pdfDate` helper), `render() -> bytes` writing the PDF object / xref structure by hand with FlateDecode content streams (via `compress`). Standard-14 fonts; PDF points, 0-255 RGB colour. Byte-identical output (no auto timestamps - golden-test friendly), `qpdf`-clean; both binaries. A writer, not a reader (no embedded fonts / images yet). |
| [`statsd.j`](../docs/modules/statsd.md) | No | A fire-and-forget StatsD metrics client over UDP: `client` / `clientWith` -> `Client` (agent address + optional metric-name prefix), then `count` / `increment` / `decrement` (counter), `gauge`, `timing` (ms), `set` each format and send one `metric:value\|type` datagram. The push counterpart to a pull-based scrape; UDP means no reply and no error when no agent listens. Integer values, no sample rates / tags yet. Needs the default binary (net). |
| [`password.j`](../docs/modules/password.md) | Yes | Generate / validate / score passwords against a policy `Schema` (character classes, length range, per-class minimums, symbol set, exclude-ambiguous glyphs). `schema` + `withLength` / `withClasses` / `withMinimums` / `withSymbolSet` / `withoutAmbiguous` builders; `generate -> string` (throws on an infeasible schema), `validate -> Report{valid, reasons}`, `complexity -> Strength{length, classes, poolSize, entropy, label}` (entropy = length * log2(pool), banded very weak..very strong). Randomness is `math`'s non-crypto RNG (like `uuid`; swaps to `crypto` later - not for high-value secrets yet). Pure `.j` over `math` / `strings` / `lists` / `convert`; both binaries. |
| [`influxdb.j`](../docs/modules/influxdb.md) | No | An InfluxDB 1.x time-series client over `http`. Build line-protocol points with value-semantic builders (`point` / `tag` / `field` / `intField` / `stringField` / `boolField` / `at` / `atTime`; mixed field types held as pre-rendered fragments), render with `line`, and `write(client, points)` them to `/write`. `query(client, influxql) -> Result` runs InfluxQL and parses the tabular JSON into a `list of Series` (name, tags, columns, stringified rows) - the same read-a-parsed-result shape as `prometheus`. Line-protocol escaping automatic; Basic auth; nanosecond precision. Over `http` + `json` + `time` + `encoding`. Needs the default binary (net). |
| [`slack.j`](../docs/modules/slack.md) | No | Post to a Slack Incoming Webhook over `http` (sibling of `gotify` / `discord`). `send(webhookUrl, text)` posts `{"text": ...}`; the `Message` builder (`message` / `text` / `section` / `header` / `divider`) assembles a Block Kit payload posted by `sendMessage`, with `render` giving the JSON. Strings JSON-escaped via `json`. Over `http` + `json`. Needs the default binary (net). |
| [`discord.j`](../docs/modules/discord.md) | No | Post to a Discord channel Webhook over `http` (sibling of `gotify` / `slack`). `send(webhookUrl, content)` posts `{"content": ...}`; the `Message` builder (`message` / `content` / `embed(m, title, description, color)`) assembles an embeds payload posted by `sendMessage`, with `render` giving the JSON. `color` is a decimal RGB int; strings JSON-escaped. Over `http` + `json`. Needs the default binary (net). |
| [`telegram.j`](../docs/modules/telegram.md) | No | A Telegram Bot API client over `http` + `json`. `bot(token)` / `botWith(token, baseUrl)` -> `Bot`; `getMe -> User`; `sendMessage` / `sendMessageWith` (parse mode) / `sendPhoto` (by URL) / `sendChatAction` -> `Message` / `bool`; `getUpdates(bot, offset, timeout) -> list of Update` long-polls for the stateful receive loop (advance `offset` past each `updateId`). Form-encoded params to `baseUrl/bot<token>/<method>`; `{"ok":false}` throws `Error{kind:"telegram"}`. Larger than the one-shot notifiers. Needs the default binary (net). |
| [`websocket.j`](../docs/modules/websocket.md) | No | An RFC 6455 WebSocket client over `net`. `connect(url)` / `connectWith(url, timeoutMs)` do the HTTP Upgrade handshake and verify the server's `Sec-WebSocket-Accept` (SHA-1 + base64 of a random client key); `send` / `sendBytes` write masked text / binary frames, `receive -> Message` reads the next message (transparently answering pings with pongs and reassembling fragmented messages), `ping`, `close`. `ws://` is plain TCP, `wss://` TLS. Binary framing + client masking with the bitwise operators over `hash` (SHA-1) + `encoding` + `math`. Needs the default binary (net). |
| [`bloom.j`](../docs/modules/bloom.md) | Yes | A Bloom filter (probabilistic set): `new(size, hashes)` / `add` / `addAll` / `mightContain`. No false negatives (a member always reports present), possible false positives. Bits packed into `bytes`; the k positions per item come from double-hashing one SHA-256 digest (`pos_i = (h1 + i*h2) mod size`). Value-semantic (`add` returns a fresh filter). Over `hash` + `bytes`; both binaries. |
| [`ringbuffer.j`](../docs/modules/ringbuffer.md) | Yes | A fixed-capacity ring buffer of strings (bounded FIFO, overwrite-oldest when full): `new(capacity)` / `push` / `pop` / `first` / `last` / `size` / `capacity` / `isEmpty` / `isFull` / `toList`. Value-semantic (read the oldest with `first` before you `pop` it, since a value-semantic pop can't return both). A sliding window of recent items. Over `lists`; both binaries. |
| [`mikrotik.j`](../docs/modules/mikrotik.md) | No | A MikroTik RouterOS API client over `net` (binary API, not SSH; plain TCP 8728 or api-ssl 8729 via TLS). `connect(options(host, user, password))` logs in (plaintext, with an automatic MD5 challenge-response fallback for pre-6.43 routers). `talk(s, command, attrs) -> list of map of string to string` runs a command and folds each `!re` reply into a row map; `print(s, path)` is read sugar for `path + "/print"`; `run(s, command, attrs) -> string` is for add / set / remove (returns the `!done` `=ret=`). Sentence-based framing with RouterOS's variable-length word length codec, hand-built from `bytes` + the bitwise operators. A `!trap` / `!fatal` throws `Error{kind:"mikrotik"}`. Over `net` + `hash` + `encoding`. Needs the default binary (net). |
| [`multipart.j`](../docs/modules/multipart.md) | Yes | Build and parse `multipart/form-data` (RFC 7578) - the file-upload counterpart to `mime`. `field(name, value)` / `file(name, filename, contentType, dataBytes)` build `Part`s; `build(parts)` (fresh random boundary) / `buildWith(parts, boundary)` -> `Built{contentType, body}`; `parse(contentType, body) -> list of Part`; `text(part)` / `isFile(part)`. Bodies are `bytes` and the delimiter is matched at `CRLF--boundary`, so binary file content round-trips intact. Pure `.j` over `strings` + `bytes`; both binaries. |
| [`amqp.j`](../docs/modules/amqp.md) | No | An AMQP 0-9-1 client for RabbitMQ over `net` - the largest protocol module. `connect(options(host, user, password))` runs the connection / channel handshake (protocol header, `Connection.Start`/`Start-Ok` with SASL PLAIN, `Tune`/`Tune-Ok`, `Open`/`Open-Ok`, `Channel.Open`); `declareQueue(c, name, durable) -> QueueInfo`; `publish` / `publishText(c, exchange, routingKey, ...)` send method + content-header + body frames; `get(c, queue, autoAck) -> Message` pulls the next message with a synchronous `Basic.Get`; `ack(c, deliveryTag)`; `close`. All frame / method / field encoding hand-built from `bytes` + the bitwise operators. Single channel, SASL PLAIN, no TLS. Needs the default binary (net). |

A new module also earns a bullet in the **Module library** section of
[`JENNIFER.md`](../JENNIFER.md) so an AI assistant writing Jennifer discovers it.
